# PRD.md: AI工作台产品需求文档 (v1.0)

## 1. 项目概述

AI工作台是一款面向知识工作者、开发者和高效人士的生产力工具，旨在通过AI能力提升用户在笔记整理、任务管理、项目协作和时间管理等方面的效率。

## 2. 目标用户

-   **知识工作者**: 需要整理大量碎片化信息、撰写报告和管理任务的白领、学生、研究人员。
-   **开发者**: 需要快速记录灵感、管理个人项目和使用AI辅助编程的用户。
-   **高效人士**: 追求极致效率，希望将AI能力无缝集成到日常工作流中的高级用户。

## 3. 核心功能需求 (MVP v1.0)

### 3.1 AI记事本与Todo List
-   **F-1.1 智能整理**:
    -   **用户故事**: 用户在自由文本框中输入一段包含会议纪要、灵感和待办事项的混合文本。
    -   **需求**: 用户点击“智能整理”按钮后，系统调用AI模型，将文本自动格式化为结构清晰的笔记（如使用Markdown标题、列表）。
-   **F-1.2 待办任务提取**:
    -   **用户故事**: 在进行智能整理时，用户希望系统能自动识别出文本中的待办任务。
    -   **需求**: AI模型需识别出如“明天需要完成XX报告”、“记得联系XX”等任务型语句，并自动将这些任务添加到独立的“Todo List”页面，任务状态默认为“待办”。
-   **F-1.3 页面布局**:
    -   **需求**: 整体UI布局参考 `https://www.biji.com/note`，采用多栏布局，如：左侧为笔记/分类列表，中间为编辑器，右侧为辅助信息或AI操作面板。

### 3.2 AI对话
-   **F-2.1 模型集成**:
    -   **需求**: 实现一个标准的AI对话界面。初期默认集成Kimi大模型，后续架构应支持快速扩展其他模型（如GPT系列、文心一言等）。
-   **F-2.2 模型管理**:
    -   **需求**: 在系统设置中，提供一个“模型管理”入口。用户点击后可以进入配置页面。
-   **F-2.3 API Key配置**:
    -   **需求**: 在模型管理页面，用户可以输入并保存自己的Kimi API Key。此Key仅保存在用户本地的 `.env` 文件中，不上传至服务器，以确保安全。

### 3.3 番茄钟 ✅ (已完成 - 2025-09-22)
-   **F-3.1 核心功能**:
    -   **需求**: 提供一个标准的番茄钟功能，包括25分钟工作、5分钟短休息、15分钟长休息的循环。支持自定义时长。
    -   **实现状态**: ✅ 已完整实现 - 2025-09-22
    -   **技术栈**: React + TypeScript + Zustand + FastAPI + PostgreSQL
    -   **测试覆盖**: 后端92% (71/71测试通过)，前端84.74%组件覆盖率
    -   **性能指标**: 计时精度±1秒，响应时间<100ms，实时后端同步
    -   **API端点**: 9个完整端点（会话、设置、统计、推荐）

-   **F-3.2 页面设计**:
    -   **需求**: 参考业内成熟的番茄钟网站，设计美观、简洁、专注的界面。
    -   **实现状态**: ✅ 已实现，采用渐变背景和进度环设计
    -   **特色功能**:
      - 智能会话切换（工作→短休息→工作→长休息）
      - 实时统计面板（总番茄钟、专注时间、连续天数）
      - 后端数据同步和会话持久化
      - 响应式设计，支持移动端
      - 声音和浏览器通知
      - 可自定义时长和自动开始选项

### 3.4 项目管理
-   **F-4.1 Trello风格看板**:
    -   **需求**: 实现一个看板（Board）式的项目管理系统。
    -   **核心元素**: 看板（Board）、列表（List，如“待办”、“进行中”、“已完成”）和卡片（Card）。
    -   **操作**: 支持卡片的拖拽、编辑、添加描述和截止日期。
    -   **设计**: 整体视觉风格和交互逻辑借鉴 `trello.com`。

### 3.5 全局主题设置
-   **F-5.1 主题切换入口**:
    -   **需求**: 在系统设置或全局导航栏提供一个明显的主题切换入口。
-   **F-5.2 默认主题 - 苹果简约风**:
    -   **需求**: 系统默认主题。设计细节见 `DESIGN.md`。
-   **F-5.3 备选主题**:
    -   **需求**: 提供额外2-3个备选主题，具体风格由设计师调研2025年流行趋势后确定。
-   **F-5.4 全局生效**:
    -   **需求**: 用户选择任一主题后，整个应用（包括记事本、对话、番茄钟、项目管理等所有页面）的UI风格应立即统一变更。

## 4. 非功能性需求
-   **性能**: 页面加载速度快，AI功能响应及时。
-   **安全性**: 用户API Key等敏感信息必须安全存储。
-   **易用性**: 用户界面直观，无需学习成本即可上手。
-   **可扩展性**: 架构设计应易于未来增加新的AI功能模块和接入新模型。

## 5. 技术规格与架构设计

### 5.1 技术栈选择

#### 5.1.1 前端技术栈
-   **框架**: Next.js 15 (React 18) with TypeScript
-   **状态管理**: Zustand (轻量级状态管理)
-   **UI组件库**: Tailwind CSS + Headless UI
-   **拖拽交互**: @dnd-kit (现代化拖拽库)
-   **Markdown渲染**: react-markdown + remark-gfm
-   **图标**: Lucide React
-   **构建工具**: Next.js内置构建系统

#### 5.1.2 后端技术栈
-   **框架**: FastAPI (Python 3.11+)
-   **数据库**: PostgreSQL 15 (主数据库) + Redis (缓存)
-   **ORM**: SQLAlchemy 2.0 + Alembic (数据库迁移)
-   **身份验证**: JWT + python-jose
-   **API文档**: FastAPI自动生成OpenAPI文档
-   **任务队列**: Celery + Redis (异步任务处理)
-   **文件存储**: 本地文件系统 + 可选S3兼容存储

#### 5.1.3 AI模型集成
-   **主要模型**: Kimi API (Moonshot AI)
-   **API调用**: 原生HTTP API + 重试机制
-   **模型配置**: 支持多模型切换的插件化架构
-   **上下文管理**: 滑动窗口机制，最大支持128K tokens
-   **速率限制**: 内置请求队列和速率限制器

### 5.2 详细功能规格

#### 5.2.1 智能整理算法 (F-1.1)
**处理流程**:
1. 文本预处理：移除多余空行、标准化标点符号
2. 段落分割：基于换行符和语义边界进行智能分段
3. 结构识别：使用AI模型识别内容类型（会议记录、灵感、任务等）
4. Markdown格式化：
   - 标题识别：包含"会议"、"总结"等关键词的行转为##标题
   - 列表识别：以数字、符号开头的行转为列表项
   - 重点标记：包含"重要"、"关键"等词汇的句子加粗处理

**AI提示词模板**:
```
请将以下文本整理成结构清晰的Markdown格式：
- 识别并添加适当的标题层级
- 将相关要点整理为列表
- 突出重要信息
- 保持原意不变

原始文本：{user_text}
```

#### 5.2.2 Todo任务提取规则 (F-1.2)
**任务识别模式**:
-   **时间关键词**: "今天"、"明天"、"本周"、"月底前"等
-   **动作动词**: "完成"、"处理"、"联系"、"准备"、"提交"等
-   **责任暗示**: "我需要"、"必须"、"记得"等
-   **项目关键词**: "报告"、"会议"、"邮件"、"文档"等

**提取算法**:
1. 正则表达式初筛：匹配包含时间+动作的组合模式
2. AI语义分析：确认是否为真实任务而非描述性语句
3. 任务标准化：统一格式为"[时间] + [动作] + [对象]"
4. 优先级判断：基于关键词自动设置优先级(P0/P1/P2)

#### 5.2.3 AI对话界面交互 (F-2.1)
**界面布局**:
-   左侧边栏：对话历史列表（支持搜索和归档）
-   主区域：当前对话内容展示
-   底部输入框：支持多行文本输入和快捷键发送

**交互流程**:
1. 用户输入消息后，显示本地消息气泡
2. 调用AI API，显示加载状态
3. 收到响应后，流式展示AI回复内容
4. 支持对话中断、重新生成、复制等功能

**状态管理**:
-   当前对话ID和消息列表
-   API调用状态（idle/loading/success/error）
-   输入内容草稿（自动保存）

#### 5.2.4 番茄钟状态管理 (F-3.1)
**状态机设计**:
```
[空闲] → 开始工作 → [工作中(25min)] → 时间到 → [待确认休息]
                                        ↓
[休息中(5min)] ← 开始短休息 ← 确认休息 ←
   ↓ 时间到
[长休息(15min)] ← 开始长休息 ← 完成4个周期
```

**计时精度**: 秒级精度，使用Web Worker确保后台运行
**通知机制**: 浏览器通知 + 声音提醒 + 页面标题闪烁
**数据记录**: 每次完成的番茄钟记录到数据库，支持统计分析

#### 5.2.5 看板拖拽交互细节 (F-4.1)
**拖拽行为**:
-   卡片拖拽：支持跨列表拖拽，实时更新位置
-   列表重排：支持列表间的顺序调整
-   拖拽预览：半透明预览效果，显示放置位置

**数据同步**:
-   拖拽开始时锁定操作（防止冲突）
-   拖拽完成后立即更新本地状态
-   后台异步保存到服务器
-   失败时回滚并提示用户

### 5.3 数据流设计

#### 5.3.1 用户数据存储结构
```sql
-- 用户表
CREATE TABLE users (
    id UUID PRIMARY KEY,
    email VARCHAR(255) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- 笔记表
CREATE TABLE notes (
    id UUID PRIMARY KEY,
    user_id UUID REFERENCES users(id),
    title VARCHAR(255),
    content TEXT,
    is_deleted BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- 任务表
CREATE TABLE todos (
    id UUID PRIMARY KEY,
    user_id UUID REFERENCES users(id),
    title VARCHAR(255) NOT NULL,
    description TEXT,
    status VARCHAR(50) DEFAULT 'pending',
    priority VARCHAR(10) DEFAULT 'P1',
    due_date TIMESTAMP,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- 看板表
CREATE TABLE boards (
    id UUID PRIMARY KEY,
    user_id UUID REFERENCES users(id),
    name VARCHAR(255) NOT NULL,
    description TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);
```

#### 5.3.2 API请求响应流程
**标准响应格式**:
```json
{
    "success": true,
    "data": {},
    "message": "操作成功",
    "error_code": null,
    "timestamp": "2025-01-01T00:00:00Z"
}
```

**错误处理机制**:
-   客户端错误 (4xx): 直接显示错误消息给用户
-   服务器错误 (5xx): 显示通用错误消息，记录详细日志
-   AI API错误: 自动重试3次，失败后显示友好的错误提示

#### 5.3.3 数据同步机制
-   **实时同步**: WebSocket连接用于实时更新（协作场景）
-   **离线支持**: 本地IndexedDB缓存，网络恢复后自动同步
-   **冲突解决**: 最后写入 wins 策略，记录操作历史

### 5.4 性能指标要求

#### 5.4.1 页面加载性能
-   **首屏加载时间**: ≤ 2秒 (移动端), ≤ 1.5秒 (桌面端)
-   **交互响应时间**: ≤ 100ms (用户操作到界面反馈)
-   **AI功能响应**:
    - 智能整理: ≤ 3秒
    - 对话响应: ≤ 2秒首包，流式输出

#### 5.4.2 系统容量指标
-   **并发用户**: 支持1000个并发用户
-   **API吞吐量**: 1000 requests/second
-   **数据存储**: 支持10万条笔记，50万条任务记录
-   **文件上传**: 单个文件最大10MB，支持批量上传

#### 5.4.3 资源使用限制
-   **内存占用**: 前端单页内存 ≤ 100MB
-   **CPU使用**: 后台任务CPU占用 ≤ 30%
-   **存储空间**: 用户数据存储不限量，但单文件受限

### 5.5 安全要求

#### 5.5.1 API Key安全存储
-   **本地存储**: 使用electron-store或localStorage加密存储
-   **加密算法**: AES-256-GCM加密，密钥基于用户密码派生
-   **访问控制**: API Key仅在需要时解密，使用后立即清理内存
-   **传输安全**: 所有API调用使用HTTPS，支持证书锁定

#### 5.5.2 用户数据隐私保护
-   **数据加密**: 敏感数据(如笔记内容)在数据库中加密存储
-   **访问日志**: 记录所有数据访问操作，支持审计
-   **数据备份**: 定期自动备份，备份数据同样加密
-   **删除策略**: 用户删除数据后立即标记，30天后永久删除

#### 5.5.3 访问权限控制
-   **身份验证**: JWT Token，有效期24小时，支持刷新
-   **权限模型**: 基于角色的访问控制(RBAC)
-   **API限流**: 基于用户ID的API调用频率限制
-   **CORS策略**: 严格的白名单制度，禁止跨域访问

#### 5.5.4 安全审计要求
-   **代码审计**: 定期进行依赖库安全扫描
-   **渗透测试**: 每季度进行一次安全测试
-   **漏洞响应**: 24小时内响应安全漏洞报告
-   **合规要求**: 遵循GDPR、网络安全法等法规要求